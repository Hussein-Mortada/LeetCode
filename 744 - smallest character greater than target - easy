//java
class Solution {
    public char nextGreatestLetter(char[] letters, char target) {
        for(int i=0;i<letters.length;i++){
            if(String.valueOf(letters[i]).compareTo(String.valueOf(target))>0){
                return letters[i];
            }
        }
        return letters[0];
    }
}
